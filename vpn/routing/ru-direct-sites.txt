# Optional extra DirectSites (one per line). Prefer regexp/.ru in base.json —
# only add non-.ru hosts here (vk.com, streaming, etc.).
# Lines starting with # are ignored.

# already in base: vk.com, okko.tv, rt.com, premier.one
