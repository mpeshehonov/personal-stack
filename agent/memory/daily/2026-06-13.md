# Daily Log 2026-06-13

## Summary

## Daily agent run complete

### Health
Site is up. RAM ~800 MB free (full mode). Docker services running. Public URLs return 200.

### Site — deployed 3 items
1. **SB-06** — Section spacing utilities (`.section`, `.section-intro`, `.section-desc`) applied across homepage sections  
2. **SB-10** — `AvailabilityBadge` in hero + footer; driven by `availability` in resume JSON (`open` / `busy`)  
3. **SB-14** — schema.org `Person` JSON-LD in locale layout  

Live check: JSON-LD and “Открыт к предложениям” badge confirmed on https://mpeshekhonov.ru/ru

### Finance — proposals for risk engine
13 markets scanned (azuro: 10, cex: 3) → 3 passed filters (CEX only). Paper day **3/7** — 9 trades, $225 total.

```json
{"market_id": "BTCUSDT", "side": "buy", "size_usd": 25, "reason": "Daily scan (cex) — low-size exploratory"}
{"market_id": "ETHUSDT", "side": "buy", "size_usd": 25, "reason": "Daily scan (cex) — low-size exploratory"}
{"market_id": "SOLUSDT", "side": "buy", "size_usd": 25, "reason": "Daily scan (cex) — low-size exploratory"}
```

M1 progress: $0 / $1,000 (108 days left).

### Bug bounty — drafts only
- 8 new GHSA advisory drafts (#15–#22): esbuild, @grpc/grpc-js, OpenZeppelin wizard, Element Call, etc.  
- Program suggestion **#14: Stripe** (HackerOne) — good M2 candidate for payments/API research  
- No submissions; awaiting Telegram `/approve bounty <id>`

### Memory
Updated `agent/memory/daily/2026-06-13.md` with all sections. Site changes are on disk and deployed; git h


Full-mode run (RAM ~800 MB free). Site healthy; deployed 3 backlog items (SB-06, SB-10, SB-14): section spacing utilities, footer/hero availability badge, JSON-LD Person schema. Finance scan: 13 markets → 3 tradeable (CEX only today); 3 paper trades logged ($75). Paper phase day **3/7**. Bounty: 8 new advisory drafts + Stripe program suggestion pending review.

## Site

**Health: OK**

| Metric | Value |
|--------|-------|
| CPU | 2.0% |
| RAM | 46.9% (~800 MB free) |
| Disk | 25% used |
| Docker | caddy, site, redis — all running |
| Public `/ru/resume` | HTTP 200 |
| PDF `/ru/resume/download` | HTTP 200 |

**Deployed (SB-06, SB-10, SB-14):**
- `.section` / `.section-intro` / `.section-desc` spacing utilities in `globals.css`; homepage sections refactored
- `AvailabilityBadge` component + `availability` field in resume JSON (`open` / `busy`); shown in hero and footer
- `buildPersonJsonLd()` — schema.org Person in locale layout `<head>`
- Verified live: JSON-LD present, badge renders on https://mpeshekhonov.ru/ru

## Finance

**Scan:** azuro=10, cex=3 → 13 scanned, 3 after filters, 10 rejected (liquidity/drift rules).

**Milestone:** M1 $0 / $1,000 (0%) — 108 days left, ~$9.26/day needed. Annual $0 / $15,000.

**Paper trades (day 3/7):**

| Day | Count | USD |
|-----|-------|-----|
| 2026-06-11 | 3 | $75 |
| 2026-06-12 | 3 | $75 |
| 2026-06-13 | 3 | $75 |
| **Total** | **9** | **$225** |

**Proposals for risk engine (paper mode — approved):**

```json
{"market_id": "BTCUSDT", "side": "buy", "size_usd": 25, "reason": "Daily scan (cex) — low-size exploratory"}
{"market_id": "ETHUSDT", "side": "buy", "size_usd": 25, "reason": "Daily scan (cex) — low-size exploratory"}
{"market_id": "SOLUSDT", "side": "buy", "size_usd": 25, "reason": "Daily scan (cex) — low-size exploratory"}
```

All Azuro markets filtered out today; CEX spot pairs passed signal rules.

## Bug Bounty

**Research only — no submissions.**

- Disclose.io: 50 active bounty programs fetched
- GHSA scan: 8 new advisory drafts (#15–#22) — npm ecosystem, high severity (esbuild, @grpc/grpc-js, OpenZeppelin wizard, Element Call, etc.)
- Program suggestion #14: **Stripe** (HackerOne) — payments API/dashboard; aligns with M2 bounty lane and JS stack
- Notable advisories for manual review: GHSA-gv7w-rqvm-qjhr (esbuild Deno integrity), GHSA-99f4-grh7-6pcq (@grpc/grpc-js crash)

Awaiting `/approve bounty <id>` before any submission.

## Lessons

- Resume JSON `availability` must go through `parseAvailability()` — raw JSON import types as `string`, breaks Next.js build otherwise.
- CEX-only pass days are normal during paper phase; Azuro rejection rate tracks liquidity/drift filters, not scan failure.
