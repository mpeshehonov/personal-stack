# Daily Log 2026-06-14

## Итог

## Daily run 2026-06-14 — завершён

### Здоровье
Сайт OK: Docker (site, caddy, redis) работает, публичные URL отвечают. RAM ~812 MB свободно — full mode.

### Сайт — 2 пункта бэклога

**SB-07 — Scroll-анимации**
- Новый компонент `FadeIn` с `framer-motion` и `useReducedMotion`
- Анимации на Hero, Selected Work, Experience, Skills, `/projects`
- `SkillGrid` тоже отключает анимации при `prefers-reduced-motion`

**SB-11 — Contact CTA на всех страницах**
- `ContactCTA` перенесён в `[locale]/layout.tsx` (перед Footer)
- Дубликат убран с homepage
- Проверено live: CTA есть на `/ru/projects` и `/ru/resume`

Deploy: `scripts/redeploy-site.sh` — build OK.

### Финансы

Скан: **13 рынков** (azuro 10 + cex 3) → **3** после фильтров (только CEX).

Paper phase **день 4/7** (IB-03): 18 сделок, **$450** total.

M1: **$0 / $1 000** (107 дней).

```json
{"market_id": "BTCUSDT", "side": "buy", "size_usd": 25, "reason": "Daily scan (cex) — low-size exploratory"}
{"market_id": "ETHUSDT", "side": "buy", "size_usd": 25, "reason": "Daily scan (cex) — low-size exploratory"}
{"market_id": "SOLUSDT", "side": "buy", "size_usd": 25, "reason": "Daily scan (cex) — low-size exploratory"}
```

### Bug bounty
Scanner не дублировался. 0 pending drafts; последние GHSA #26–#30 на review. Stripe (#14) — ждёт `/approve bounty <id>`.

### Git
- Site: `feat(site): scroll animations and global contact CTA` — закоммичено
- Daily log: `agent/memory/daily/2026-06-14.md` — для orchestrator

### Следующие кандидаты из бэ


Full-mode run (RAM ~812 MB free). Сайт OK. Задеплоены **SB-07** (scroll-анимации через `FadeIn`, `prefers-reduced-motion`) и **SB-11** (ContactCTA на всех страницах через layout). Finance: 13 рынков → 3 CEX, paper day **4/7** (18 сделок, $450). Bounty: orchestrator-only, новых submit нет.

## Сайт

**Health: OK**

| Metric | Value |
|--------|-------|
| CPU | 2.0% |
| RAM | 50.6% (~812 MB free) |
| Docker | site, caddy, redis — up |
| Public URL | 307 → /ru (OK) |

**Deployed (2 items):**

1. **SB-07** — компонент `FadeIn` (framer-motion + `useReducedMotion`); анимации на Hero, Selected Work, Experience, Skills, `/projects`. SkillGrid тоже уважает reduced-motion.
2. **SB-11** — `ContactCTA` в `[locale]/layout.tsx` перед Footer на всех страницах; дубликат убран с homepage.

Live: https://mpeshekhonov.ru/ru

## Финансы

Скан: azuro 10 + cex 3 = **13 рынков** → **3** после фильтров (CEX only; Azuro отфильтрован по liquidity/drift).

Paper phase **день 4/7** (IB-03):

| День | Сделок | USD |
|------|--------|-----|
| 2026-06-11 | 3 | $75 |
| 2026-06-12 | 3 | $75 |
| 2026-06-13 | 9 | $225 |
| 2026-06-14 | 3 | $75 |
| **Итого** | **18** | **$450** |

M1: **$0 / $1 000** (107 дней, ~$9.35/день нужно).

```json
{"market_id": "BTCUSDT", "side": "buy", "size_usd": 25, "reason": "Daily scan (cex) — low-size exploratory"}
{"market_id": "ETHUSDT", "side": "buy", "size_usd": 25, "reason": "Daily scan (cex) — low-size exploratory"}
{"market_id": "SOLUSDT", "side": "buy", "size_usd": 25, "reason": "Daily scan (cex) — low-size exploratory"}
```

## Баг-баунти

Semi-auto режим — дублирование scanner.py не выполнялось. В очереди 0 pending; последние drafts (#26–#30): GHSA esbuild/grpc-js/OpenZeppelin/Element Call. Program suggestion **Stripe** (#14) — на review, submit только после `/approve bounty <id>`.

## Уроки

- `FadeIn` как client wrapper позволяет оставлять page-компоненты server-side и централизованно уважать `prefers-reduced-motion`.
- ContactCTA в layout — один блок на все роуты без дублирования на homepage.
